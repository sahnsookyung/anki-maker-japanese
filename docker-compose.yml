services:
  backend:
    image: python:3.11-slim
    working_dir: /app/backend
    command: sh -c "pip install uv && uv sync --group dev --extra ocr && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  web:
    image: node:22-slim
    working_dir: /app/apps/web
    command: sh -c "npm install && npm run dev -- --hostname 0.0.0.0"
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
    volumes:
      - .:/app
    ports:
      - "3000:3000"
